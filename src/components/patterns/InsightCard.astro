---
interface Props { title: string; summary: string; category: string; author: string; href: string; publishedDate?: Date; variant?: 'standard' | 'featured' | 'compact'; }
const { title, summary, category, author, href, publishedDate, variant = 'standard' } = Astro.props;
const formattedDate = publishedDate?.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' });
---
<article class:list={['insight', `insight--${variant}`]}>
  <div class="insight__meta"><span>{category}</span>{formattedDate && <><span aria-hidden="true">·</span><time datetime={publishedDate?.toISOString()}>{formattedDate}</time></>}</div>
  <h3><a href={href}>{title}</a></h3>
  {variant !== 'compact' && <p class="insight__summary">{summary}</p>}
  <p class="insight__author">By {author}</p>
</article>
<style>
  .insight { display: grid; gap: var(--space-md); border-block-start: var(--border-width-hairline) solid var(--border); padding-block: var(--card-padding); }
  .insight--featured { border-block-start-color: var(--accent); }
  .insight__meta { display: flex; flex-wrap: wrap; gap: var(--space-xs); color: var(--primary); font-size: var(--type-caption-size); font-weight: var(--type-caption-weight); letter-spacing: var(--type-caption-tracking); line-height: var(--type-caption-line-height); text-transform: uppercase; }
  .insight h3, .insight__summary, .insight__author { margin: 0; }
  .insight h3 { font-size: var(--type-heading-4-size); font-weight: var(--type-heading-4-weight); line-height: var(--type-heading-4-line-height); }
  .insight--featured h3 { font-size: var(--type-heading-3-size); line-height: var(--type-heading-3-line-height); }
  .insight h3 a { color: inherit; text-decoration-color: transparent; text-underline-offset: var(--space-xs); transition: color var(--duration-fast) var(--ease-standard), text-decoration-color var(--duration-fast) var(--ease-standard); }
  .insight h3 a:hover { color: var(--primary); text-decoration-color: var(--accent); }
  .insight__summary { color: var(--muted-foreground); line-height: var(--type-body-line-height); }
  .insight__author { font-size: var(--type-body-sm-size); font-weight: var(--type-label-weight); }
</style>
